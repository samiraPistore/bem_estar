import 'package:flutter/material.dart';

class CardConcluidos extends StatelessWidget {
  final String numDesafio;
  final String titulo;
  final String status;
  const CardConcluidos({super.key, required this.numDesafio, required this.titulo, required this.status});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Card( 
        child: Column(
          
        ),
      )
    );
  }
}